#!/usr/bin/env bash
# bin/release <build-dir>

cat << EOF
addons: []
default_process_types:
  web: npm start --max_old_space_size=$(( $(jq -r '.limits.mem' <<<"$VCAP_APPLICATION") * 75 / 100 )) $NPM_CLI_OPTIONS
EOF
